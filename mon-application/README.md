# Payvora (formerly Facture Pro)

This is the Payvora React + Vite front-end application.

Quick commands:

- Install: `npm install`
- Development: `npm run dev`
- Build: `npm run build`

Branding: The app name is `Payvora` and includes a generated `public/logo.png` used as the favicon and apple touch icon.

